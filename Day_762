Now I need to actually run the Phase 1 test matrix against both implementations, not just eyeball it. Let me set up a real side-by-side test using JVM-available regex (Kotlin's Pattern is Java's, so I can test the exact same patterns via Python's re module and a small Java-regex-in-Python bridge isn't available — instead I'll test via kotlinc if available, or fall back to careful manual verification with realistic sample SMS text run through Python directly, then manually trace the Kotlin patterns against the same strings).
