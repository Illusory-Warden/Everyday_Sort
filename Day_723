If more errors appear after build
Most likely culprits and fixes:
ErrorFixcannot find symbol setPhone in TeacherSame fix — add phone field to Teacher.javafindByBatch not found in StudentRepositoryAdd List<Student> findByBatch(Batch batch) to StudentRepositoryfindByUser not foundAdd Optional<Student> findByUser(User user) to StudentRepositoryAny other getDisplayId / setDisplayIdCtrl+Shift+R replace with getLoginId / setLoginId
